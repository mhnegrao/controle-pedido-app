const database= require('../../database/configDatabase');

class DataDb {
    insert(item) {}
    update(id) {}
    delete(id) {}
    select(expression) {}
    find(){}
    findById(id)
}


modules.export = new DataDb()